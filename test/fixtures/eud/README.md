# Self-authored EUD smoke source

`minimal-smoke.eps` is a project-authored epScript fixture used only by the
opt-in real euddraft smoke test. It adds a no-op memory action so the compiler
must emit an EUD payload without changing intended gameplay state.

The fixture is covered by the repository MIT license and has no third-party
map or script content.
